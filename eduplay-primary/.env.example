# Copy this to .env.local and fill in your values
# The Vite proxy (vite.config.ts) forwards /api → Django in development,
# so VITE_API_URL is optional locally. Set it for production deploys.

VITE_API_URL=http://localhost:8000/api
