name: Legacy Main Workflow (Disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is intentionally disabled. Use deploy.yml, backend.yml, primary.yml, secondary.yml, and teacher.yml."
